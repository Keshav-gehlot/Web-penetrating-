import React,{useState}from'react';
import{useNavigate,useSearchParams}from'react-router-dom';
const API=(import.meta.env.VITE_PHANTOM_API_URL??'http://localhost:8000').replace(/\/$/,'');
export default function AcceptInvitation(){const nav=useNavigate(),[params]=useSearchParams(),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),token=params.get('token')||'';async function submit(e:React.FormEvent){e.preventDefault();setError('');if(password!==confirm){setError('Passwords do not match');return}const r=await fetch(`${API}/api/v1/auth/invitations/accept`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json();if(!r.ok){setError(d.detail||'Invitation could not be accepted');return}setDone(true)}return <div className="min-h-screen bg-phantom-bg text-phantom-text-primary flex items-center justify-center p-6"><div className="w-full max-w-md rounded-2xl border border-phantom-border bg-phantom-surface p-6 shadow-2xl">{done?<><h1 className="text-xl font-semibold">Account activated</h1><p className="text-sm text-phantom-text-secondary mt-2">Your workspace membership is active. Sign in with the password you just created.</p><button onClick={()=>nav('/login')} className="mt-6 w-full h-11 rounded-lg bg-phantom-text-primary text-black font-semibold">Continue to sign in</button></>:<><h1 className="text-xl font-semibold">Accept PHANTOM invitation</h1><p className="text-sm text-phantom-text-secondary mt-2 mb-6">Set your password to activate your workspace account.</p><form onSubmit={submit} className="space-y-4"><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required placeholder="Password (8+ characters)" className="w-full h-11 rounded-lg border border-phantom-border bg-phantom-bg px-3"/><input value={confirm} onChange={e=>setConfirm(e.target.value)} type="password" minLength={8} required placeholder="Confirm password" className="w-full h-11 rounded-lg border border-phantom-border bg-phantom-bg px-3"/>{error&&<div className="text-xs text-phantom-coral">{error}</div>}<button disabled={!token||password.length<8} className="w-full h-11 rounded-lg bg-phantom-text-primary text-black font-semibold disabled:opacity-40">Activate account</button></form></>}</div></div>}