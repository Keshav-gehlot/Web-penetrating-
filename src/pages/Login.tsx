import React,{useState}from'react';
import{useLocation,useNavigate}from'react-router-dom';
import{Eye,EyeOff,ShieldCheck,Loader2}from'lucide-react';
import{login}from'../lib/auth';
export default function Login(){const nav=useNavigate();const loc=useLocation();const[email,setEmail]=useState('admin@phantom.local');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password);const to=new URLSearchParams(loc.search).get('next')||'/dashboard';nav(to,{replace:true})}catch(err){setError(err instanceof Error?err.message:'Authentication failed')}finally{setBusy(false)}}return <div className="min-h-screen bg-phantom-bg text-phantom-text-primary flex items-center justify-center p-6"><div className="w-full max-w-md"><div className="flex items-center gap-3 mb-8"><div className="w-11 h-11 rounded-xl border border-phantom-border bg-phantom-surface flex items-center justify-center"><ShieldCheck size={21} className="text-phantom-cyan"/></div><div><div className="font-semibold tracking-tight">PHANTOM</div><div className="text-xs text-phantom-text-tertiary">Security Operations Platform</div></div></div><div className="rounded-2xl border border-phantom-border bg-phantom-surface p-6 shadow-2xl"><h1 className="text-xl font-semibold">Sign in</h1><p className="text-sm text-phantom-text-secondary mt-1 mb-6">Authenticate to access your security workspace.</p><form onSubmit={submit} className="space-y-4"><label className="block"><span className="text-xs text-phantom-text-secondary">Email</span><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="username" className="mt-1.5 w-full h-11 rounded-lg border border-phantom-border bg-phantom-bg px-3 outline-none focus:border-phantom-cyan"/></label><label className="block"><span className="text-xs text-phantom-text-secondary">Password</span><div className="relative mt-1.5"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} autoComplete="current-password" className="w-full h-11 rounded-lg border border-phantom-border bg-phantom-bg px-3 pr-11 outline-none focus:border-phantom-cyan"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-phantom-text-tertiary">{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></label>{error&&<div className="rounded-lg border border-phantom-coral/30 bg-phantom-coral/10 px-3 py-2 text-xs text-phantom-coral">{error}</div>}<button disabled={busy} className="w-full h-11 rounded-lg bg-phantom-text-primary text-black font-semibold text-sm flex items-center justify-center gap-2 disabled:opacity-50">{busy&&<Loader2 size={16} className="animate-spin"/>}{busy?'Signing in…':'Continue'}</button></form><div className="mt-5 text-[11px] text-phantom-text-tertiary">Use the bootstrap credentials configured in the backend environment.</div></div></div></div>}
